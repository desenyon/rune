[project]
name = "rune-fixture-python-app"
version = "0.1.0"
description = "Small Python app fixture for Rune indexer tests."
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
